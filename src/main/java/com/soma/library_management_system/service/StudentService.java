package com.soma.library_management_system.service;

import com.soma.library_management_system.payload.StudentCreateRequest;

public interface StudentService {
    void create(StudentCreateRequest studentCreateRequest);
}
